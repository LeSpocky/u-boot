if TARGET_PROFICUBE

config SYS_BOARD
	default "proficube"

config SYS_VENDOR
	default "thorsis"

config SYS_SOC
	default "at91"

config SYS_CONFIG_NAME
	default "proficube"

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
	imply DISTRO_DEFAULTS

endif
